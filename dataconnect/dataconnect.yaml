specVersion: "v1"
serviceId: "netflixfiles"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netflixfiles-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
